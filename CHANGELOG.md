# 1.0.1

- Now you can pause the module to handle specific situations typing `/gp off`or `/gp on`

# 1.0.2

- It now works with tokens of any size (not just scale 1), and tokens revert to their original size.

- In the settings, you can set a threshold so it ignores any scale values greater than that number.